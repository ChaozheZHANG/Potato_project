# 部署检查清单

## 生产环境路径
`F:\seven\seven_plc_vision_app\`

## 需要复制的文件

### ✅ 已有文件（不需要复制）
- `app/cam_plc_capture.py` - 已有
- `app/plc_dummy_loop.py` - 已有

### 📦 需要新增的文件

1. **PLC寄存器定义**
   ```
   plc/
   ├── __init__.py
   └── registers.py
   ```
   复制到：`F:\seven\seven_plc_vision_app\plc\`

2. **检测服务脚本**
   ```
   scripts/
   ├── __init__.py
   └── detect_and_write_plc.py
   ```
   复制到：`F:\seven\seven_plc_vision_app\scripts\`

3. **模型文件**
   ```
   models/
   └── yolov8s-obb-potato-v22_best.pt
   ```
   复制到：`F:\seven\seven_plc_vision_app\models\`

4. **配置文件**
   ```
   config/
   └── grading_rules.json
   ```
   复制到：`F:\seven\seven_plc_vision_app\config\`

5. **启动脚本**
   ```
   run_detection_service.ps1
   ```
   复制到：`F:\seven\seven_plc_vision_app\`

## 最终目录结构

```
F:\seven\seven_plc_vision_app\
├── app\                          ✅ 已有
│   ├── cam_plc_capture.py        ✅ 已有
│   └── plc_dummy_loop.py         ✅ 已有
├── plc\                          📦 新增
│   ├── __init__.py               📦 新增
│   └── registers.py              📦 新增
├── scripts\                      📦 新增
│   ├── __init__.py               📦 新增
│   └── detect_and_write_plc.py   📦 新增
├── models\                       📦 新增
│   └── yolov8s-obb-potato-v22_best.pt  📦 新增
├── config\                       📦 新增
│   └── grading_rules.json         📦 新增
└── run_detection_service.ps1     📦 新增
```

## 验证步骤

1. **检查文件是否存在**
   ```powershell
   cd F:\seven\seven_plc_vision_app
   Test-Path app\cam_plc_capture.py      # 应该返回 True
   Test-Path app\plc_dummy_loop.py       # 应该返回 True
   Test-Path plc\registers.py            # 应该返回 True
   Test-Path scripts\detect_and_write_plc.py  # 应该返回 True
   Test-Path models\yolov8s-obb-potato-v22_best.pt  # 应该返回 True
   Test-Path config\grading_rules.json   # 应该返回 True
   ```

2. **测试导入**
   ```powershell
   cd F:\seven\seven_plc_vision_app
   python -c "from plc.registers import attrs_base; print('OK')"
   ```

3. **运行检测服务**
   ```powershell
   .\run_detection_service.ps1 -CameraDir "F:\data\camera" -PLCIp "192.168.1.1" -PLCPort 502 -Channels "1,2,3,4"
   ```

## 注意事项

- ✅ `app/` 目录已存在，不需要创建
- ✅ `app/cam_plc_capture.py` 和 `app/plc_dummy_loop.py` 不需要修改
- 📦 只需要添加新的文件和目录

